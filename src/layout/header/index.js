import React from 'react';

import style from './header.module.scss';

const Header = ({}) => {

  return (
    <header>
      <div>
        Header
      </div>
    </header>
  )
}

export default Header;